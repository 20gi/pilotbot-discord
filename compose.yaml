services:
  pilotbot:
    build: .
    container_name: pilotbot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
      - ./secrets:/run/secrets:ro
    ports:
      - "8447:8447"
